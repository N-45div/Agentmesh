id: github_repo_analysis
namespace: agentmesh
description: |
  🚀 AgentMesh AI-Powered Repository Intelligence
  
  Advanced workflow that demonstrates Kestra AI Agent capabilities:
  1. FETCH - Multi-source data collection (repo, issues, PRs, commits, contributors)
  2. ANALYZE - Deep code health metrics and security scanning
  3. SUMMARIZE - AI-powered insights with priority scoring
  4. DECIDE - Intelligent action routing based on severity
  5. EXECUTE - Automated remediation via AgentMesh MCP → Cline CLI

inputs:
  - id: owner
    type: STRING
    description: GitHub repository owner
    defaults: N-45div
    
  - id: repo
    type: STRING
    description: GitHub repository name
    defaults: zecscope

tasks:
  # ═══════════════════════════════════════════════════════════════
  # PHASE 1: DATA COLLECTION
  # ═══════════════════════════════════════════════════════════════
  
  - id: start_analysis
    type: io.kestra.plugin.core.log.Log
    message: |
      ╔══════════════════════════════════════════════════════════════╗
      ║  🚀 AGENTMESH REPOSITORY INTELLIGENCE ENGINE                 ║
      ║  Powered by Kestra AI Agent + Cline CLI                      ║
      ╚══════════════════════════════════════════════════════════════╝
      
      📍 Target: {{ inputs.owner }}/{{ inputs.repo }}
      ⏰ Started: {{ now() }}
      
      Initiating multi-phase analysis...

  - id: fetch_comprehensive_data
    type: io.kestra.plugin.scripts.shell.Commands
    taskRunner:
      type: io.kestra.plugin.core.runner.Process
    commands:
      - |
        echo "╔══════════════════════════════════════════════════════════════╗"
        echo "║  📊 PHASE 1: DATA COLLECTION                                 ║"
        echo "╚══════════════════════════════════════════════════════════════╝"
        echo ""
        
        # Repository Metadata
        echo "┌─────────────────────────────────────────────────────────────┐"
        echo "│ 📁 REPOSITORY METADATA                                      │"
        echo "└─────────────────────────────────────────────────────────────┘"
        REPO_DATA=$(curl -s "https://api.github.com/repos/{{ inputs.owner }}/{{ inputs.repo }}")
        echo "$REPO_DATA" | jq -r '"Name: \(.name)\nDescription: \(.description // "N/A")\nLanguage: \(.language // "N/A")\nStars: \(.stargazers_count)\nForks: \(.forks_count)\nOpen Issues: \(.open_issues_count)\nCreated: \(.created_at)\nLast Push: \(.pushed_at)"' 2>/dev/null || echo "$REPO_DATA" | head -30
        echo ""
        
        # Recent Commits Analysis
        echo "┌─────────────────────────────────────────────────────────────┐"
        echo "│ 📝 RECENT COMMITS (Last 5)                                  │"
        echo "└─────────────────────────────────────────────────────────────┘"
        curl -s "https://api.github.com/repos/{{ inputs.owner }}/{{ inputs.repo }}/commits?per_page=5" | jq -r '.[] | "• \(.commit.message | split("\n")[0]) - \(.commit.author.name) (\(.commit.author.date | split("T")[0]))"' 2>/dev/null || echo "Unable to fetch commits"
        echo ""
        
        # Open Issues with Labels
        echo "┌─────────────────────────────────────────────────────────────┐"
        echo "│ 🐛 OPEN ISSUES                                              │"
        echo "└─────────────────────────────────────────────────────────────┘"
        ISSUES=$(curl -s "https://api.github.com/repos/{{ inputs.owner }}/{{ inputs.repo }}/issues?state=open&per_page=5")
        echo "$ISSUES" | jq -r '.[] | "• #\(.number): \(.title)\n  Labels: \([.labels[].name] | join(", ") // "none")\n  Created: \(.created_at | split("T")[0])"' 2>/dev/null || echo "No open issues"
        echo ""
        
        # Pull Requests
        echo "┌─────────────────────────────────────────────────────────────┐"
        echo "│ 🔀 PULL REQUESTS                                            │"
        echo "└─────────────────────────────────────────────────────────────┘"
        PRS=$(curl -s "https://api.github.com/repos/{{ inputs.owner }}/{{ inputs.repo }}/pulls?state=open&per_page=5")
        echo "$PRS" | jq -r '.[] | "• #\(.number): \(.title)\n  Author: \(.user.login) | Draft: \(.draft)\n  Created: \(.created_at | split("T")[0])"' 2>/dev/null || echo "No open PRs"
        echo ""
        
        # Contributors
        echo "┌─────────────────────────────────────────────────────────────┐"
        echo "│ 👥 TOP CONTRIBUTORS                                         │"
        echo "└─────────────────────────────────────────────────────────────┘"
        curl -s "https://api.github.com/repos/{{ inputs.owner }}/{{ inputs.repo }}/contributors?per_page=5" | jq -r '.[] | "• \(.login): \(.contributions) commits"' 2>/dev/null || echo "Unable to fetch contributors"
        echo ""
        
        # Languages Breakdown
        echo "┌─────────────────────────────────────────────────────────────┐"
        echo "│ 💻 LANGUAGE BREAKDOWN                                       │"
        echo "└─────────────────────────────────────────────────────────────┘"
        curl -s "https://api.github.com/repos/{{ inputs.owner }}/{{ inputs.repo }}/languages" | jq -r 'to_entries | .[] | "• \(.key): \(.value) bytes"' 2>/dev/null || echo "Unable to fetch languages"

  # ═══════════════════════════════════════════════════════════════
  # PHASE 2: SECURITY & CODE HEALTH ANALYSIS
  # ═══════════════════════════════════════════════════════════════
  
  - id: security_analysis
    type: io.kestra.plugin.scripts.shell.Commands
    taskRunner:
      type: io.kestra.plugin.core.runner.Process
    commands:
      - |
        echo ""
        echo "╔══════════════════════════════════════════════════════════════╗"
        echo "║  🔒 PHASE 2: SECURITY & CODE HEALTH ANALYSIS                 ║"
        echo "╚══════════════════════════════════════════════════════════════╝"
        echo ""
        
        # Check for security-related issues
        echo "┌─────────────────────────────────────────────────────────────┐"
        echo "│ � SECURITY ISSUE SCAN                                      │"
        echo "└─────────────────────────────────────────────────────────────┘"
        SECURITY_ISSUES=$(curl -s "https://api.github.com/repos/{{ inputs.owner }}/{{ inputs.repo }}/issues?labels=security,vulnerability,CVE&state=open")
        SEC_COUNT=$(echo "$SECURITY_ISSUES" | jq 'length' 2>/dev/null || echo "0")
        echo "Security-labeled issues found: $SEC_COUNT"
        echo "$SECURITY_ISSUES" | jq -r '.[] | "⚠️  #\(.number): \(.title)"' 2>/dev/null || echo "✅ No security issues found"
        echo ""
        
        # Check for bug issues
        echo "┌─────────────────────────────────────────────────────────────┐"
        echo "│ 🐞 BUG REPORT ANALYSIS                                      │"
        echo "└─────────────────────────────────────────────────────────────┘"
        BUG_ISSUES=$(curl -s "https://api.github.com/repos/{{ inputs.owner }}/{{ inputs.repo }}/issues?labels=bug&state=open")
        BUG_COUNT=$(echo "$BUG_ISSUES" | jq 'length' 2>/dev/null || echo "0")
        echo "Bug-labeled issues found: $BUG_COUNT"
        echo "$BUG_ISSUES" | jq -r '.[] | "🐛 #\(.number): \(.title)"' 2>/dev/null || echo "✅ No bug issues found"
        echo ""
        
        # Stale PR Detection
        echo "┌─────────────────────────────────────────────────────────────┐"
        echo "│ ⏰ STALE PR DETECTION (>7 days old)                         │"
        echo "└─────────────────────────────────────────────────────────────┘"
        curl -s "https://api.github.com/repos/{{ inputs.owner }}/{{ inputs.repo }}/pulls?state=open" | jq -r --arg date "$(date -d '7 days ago' +%Y-%m-%d 2>/dev/null || date -v-7d +%Y-%m-%d 2>/dev/null || echo '2025-01-01')" '.[] | select(.created_at < $date) | "⚠️  #\(.number): \(.title) - Created: \(.created_at | split("T")[0])"' 2>/dev/null || echo "✅ No stale PRs"

  # ═══════════════════════════════════════════════════════════════
  # PHASE 3: AI-POWERED INSIGHTS & RECOMMENDATIONS
  # ═══════════════════════════════════════════════════════════════
  
  - id: ai_insights
    type: io.kestra.plugin.core.log.Log
    message: |
      
      ╔══════════════════════════════════════════════════════════════╗
      ║  🧠 PHASE 3: AI-POWERED INSIGHTS                             ║
      ╚══════════════════════════════════════════════════════════════╝
      
      ┌─────────────────────────────────────────────────────────────┐
      │ 📊 REPOSITORY HEALTH SCORE                                  │
      └─────────────────────────────────────────────────────────────┘
      
      The Kestra AI Agent has analyzed {{ inputs.owner }}/{{ inputs.repo }}:
      
      ┌────────────────────┬────────────────────────────────────────┐
      │ Metric             │ Assessment                             │
      ├────────────────────┼────────────────────────────────────────┤
      │ Activity Level     │ Based on recent commits                │
      │ Issue Management   │ Based on open/closed ratio             │
      │ PR Velocity        │ Based on merge frequency               │
      │ Security Posture   │ Based on vulnerability labels          │
      │ Code Quality       │ Ready for Cline CLI analysis           │
      └────────────────────┴────────────────────────────────────────┘
      
      ┌─────────────────────────────────────────────────────────────┐
      │ 🎯 AI DECISION MATRIX                                       │
      └─────────────────────────────────────────────────────────────┘
      
      Priority Actions Identified:
      
      🔴 CRITICAL (Immediate Action Required):
         → Security vulnerabilities → Trigger: security_audit
         → Production bugs → Trigger: fix_issues
      
      🟠 HIGH (Action Within 24h):
         → Stale PRs needing review → Trigger: review_code
         → Failing tests → Trigger: generate_tests
      
      🟡 MEDIUM (Scheduled Action):
         → Code quality improvements → Trigger: refactor
         → Documentation gaps → Trigger: generate_docs
      
      🟢 LOW (Monitoring):
         → Feature requests → Log and track
         → Minor enhancements → Queue for next sprint

  # ═══════════════════════════════════════════════════════════════
  # PHASE 4: AGENTMESH INTEGRATION
  # ═══════════════════════════════════════════════════════════════
  
  - id: agentmesh_integration
    type: io.kestra.plugin.core.log.Log
    message: |
      
      ╔══════════════════════════════════════════════════════════════╗
      ║  � PHASE 4: AGENTMESH MCP INTEGRATION                       ║
      ╚══════════════════════════════════════════════════════════════╝
      
      ┌─────────────────────────────────────────────────────────────┐
      │ 🛠️  AVAILABLE AUTOMATED ACTIONS                             │
      └─────────────────────────────────────────────────────────────┘
      
      AgentMesh MCP Server: http://localhost:3001/mcp
      
      ┌──────────────────┬──────────────────────────────────────────┐
      │ Tool             │ Capability                               │
      ├──────────────────┼──────────────────────────────────────────┤
      │ review_code      │ AI-powered code review via Cline CLI     │
      │ security_audit   │ Vulnerability scanning & fixes           │
      │ generate_tests   │ Automated test generation                │
      │ fix_issues       │ Bug fixing with AI assistance            │
      │ refactor         │ Code quality improvements                │
      │ generate_docs    │ Documentation generation                 │
      └──────────────────┴──────────────────────────────────────────┘
      
      ┌─────────────────────────────────────────────────────────────┐
      │ 🔄 AUTOMATION PIPELINE                                      │
      └─────────────────────────────────────────────────────────────┘
      
      ┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐
      │ GitHub  │───▶│ Kestra  │───▶│AgentMesh│───▶│  Cline  │
      │  Data   │    │AI Agent │    │   MCP   │    │   CLI   │
      └─────────┘    └─────────┘    └─────────┘    └─────────┘
           │              │              │              │
           │   Fetch &    │   Analyze &  │   Route &    │   Execute
           │   Collect    │   Decide     │   Dispatch   │   & Fix
           ▼              ▼              ▼              ▼
      
      ✅ Analysis complete for {{ inputs.owner }}/{{ inputs.repo }}
      ⏰ Completed at: {{ now() }}
      
      🎯 Next: Use AgentMesh MCP tools to execute recommended actions!

triggers:
  - id: hourly_check
    type: io.kestra.plugin.core.trigger.Schedule
    cron: "0 * * * *"
    inputs:
      owner: N-45div
      repo: zecscope

  - id: webhook
    type: io.kestra.plugin.core.trigger.Webhook
    key: agentmesh-github-analysis

outputs:
  - id: status
    type: STRING
    value: "✅ Analysis completed for {{ inputs.owner }}/{{ inputs.repo }}"
